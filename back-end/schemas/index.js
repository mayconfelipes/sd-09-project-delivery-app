const removePassword = require('./removePassword');
const generateError = require('./generateError');
const seedSalesProducts = require('./seedSalesProducts');
const calculateTotalPrice = require('./calculateTotalPrice');

module.exports = {
  removePassword,
  generateError,
  seedSalesProducts,
  calculateTotalPrice,
};
