const errorTreatment = require('./errorTreatment');
const authentication = require('./authentication');
const authenticationAttempt = require('./authenticationAttempt');

module.exports = {
  errorTreatment,
  authentication,
  authenticationAttempt,
};
