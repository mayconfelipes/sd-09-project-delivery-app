import styled from 'styled-components';
import { Link } from 'react-router-dom';

export const LinkNav = styled(Link)`
  margin: 0.2em, 2em;
  padding: 20px;
`;

export const LinkNav2 = styled(Link)`
  margin: 0.2em, 2em;
`;
