import { useState, useEffect } from 'react';

const useNavItems = () => {
  const [items, setItems] = useState(() => []);

  // useEffect(() => {
  //   effect
  // }, [])
};

export default useNavItems;
